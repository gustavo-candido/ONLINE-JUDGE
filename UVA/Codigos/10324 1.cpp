#include <bits/stdc++.h>
using namespace std;

string s;
int n, i, j, C=0;
map<int,int>m;
void process()
{
	int x, y;
	x = 0;
	for(int i=0; i<s.size(); i++) {
		if(s[i] == s[x]) continue;
		y = i;
		m.ins
	}
}
int main()
{
	while(cin >> s && !cin.eof) {
		printf("Case %d:", ++C);
		cin >> n;

		while(n--) {
			cin >> i >> j;
		}
	} 
}
