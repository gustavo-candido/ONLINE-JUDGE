#include <stdio.h>
 main(){
   float x;
   int contador=0,y;
   for (y=1;y<=6;y++){

      scanf("%f",&x);

      if(x>0){
         contador + contador;
         contador++;
      }
   }
   printf("%d valores positivos\n",contador);
}